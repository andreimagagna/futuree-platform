import React from 'react';

export default function PlaceholderComponent() {
  return (
    <div className="p-4">
      <h2 className="text-lg font-semibold">Componente placeholder</h2>
      <p className="text-sm text-muted-foreground">Arquivo substituído por um componente simples para evitar erros de compilação.</p>
    </div>
  );
}
import React from 'react';

export default function PlaceholderComponent() {
  return (
    <div className="p-4">
      <h2 className="text-lg font-semibold">Componente placeholder</h2>
      <p className="text-sm text-muted-foreground">Arquivo substituído por um componente simples para evitar erros de compilação.</p>
    </div>
  );
}
